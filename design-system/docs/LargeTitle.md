---
category: Content
---

# LargeTitle

34pt bold screen title with tight letter-spacing — the iOS large-title style. Use once at the top of a primary screen (Chats, Settings); pushed screens use the `NavBar` inline title instead.

```tsx
<LargeTitle>Settings</LargeTitle>
```
