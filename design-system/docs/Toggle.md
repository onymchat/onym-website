---
category: Controls
---

# Toggle

iOS switch (51×31), green track when on. Almost always lives in the `trailing` slot of a `Row`.

```tsx
<Row icon="bell" iconColor="red" title="Notifications" trailing={<Toggle checked onChange={…} />} />
```
